{
  "name": "investigation-graph-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "Demo-only investigation graph platform using Neo4j, semantic search and an MCP read-only facade.",
  "license": "UNLICENSED",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "smoke": "node scripts/smoke-demo.js"
  },
  "engines": {
    "node": ">=20"
  }
}

