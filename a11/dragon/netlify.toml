[build]
  base = "."
  command = "npm ci && npm run build:packages && npm --workspace @nossen/dragon-web run build"
  publish = "apps/dragon-web/dist"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
