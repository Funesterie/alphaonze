# GABARIT — ne pas committer de vraies valeurs. Remplir hors git puis appliquer,
# ou (recommandé) gérer via Sealed Secrets / External Secrets / SOPS.
apiVersion: v1
kind: Secret
metadata:
  name: a11-backend-secrets
  namespace: funesterie
type: Opaque
stringData:
  JWT_SECRET: "REPLACE_ME"
  STRIPE_SECRET_KEY: "REPLACE_ME"
  STRIPE_ROYALTIES_PRICE_ID: "REPLACE_ME"
  MOLLIE_API_KEY: "REPLACE_ME"
  PAYPAL_CLIENT_ID: "REPLACE_ME"
  PAYPAL_CLIENT_SECRET: "REPLACE_ME"
  AZURE_TENANT_ID: "REPLACE_ME"
  AZURE_CLIENT_ID: "REPLACE_ME"
  AZURE_CLIENT_SECRET: "REPLACE_ME"
  APPLICATIONINSIGHTS_CONNECTION_STRING: "REPLACE_ME"
  # Provider keys (Suno/Mureka/Comfy/etc.) — add as needed, mirroring a11.env.
