diff --git a/node_modules/@funeste38/spyder/dist/index.js b/node_modules/@funeste38/spyder/dist/index.js
index 796f952..97b1f5d 100644
--- a/node_modules/@funeste38/spyder/dist/index.js
+++ b/node_modules/@funeste38/spyder/dist/index.js
@@ -61,9 +61,13 @@ class SpyderServer {
                         const pkt = (0, packet_1.decodePacket)(buf);
                         if (!pkt)
                             return;
-                        const valid = pkt.checksum !== 0;
+                        const valid = pkt.valid === true;
                         const score = valid ? 2 : 0;
                         this.memory.add({ bits: pkt.payload, type: pkt.type, valid, score });
+                        if (!valid) {
+                            console.warn(`[SPYDER] packet checksum invalid, dropping (type=${pkt.type})`);
+                            return;
+                        }
                         if (pkt.type === 1) {
                             const resp = await this.opts.sendToA11(pkt.payload);
                             const out = (0, packet_1.encodePacket)(2, resp);
@@ -73,7 +77,7 @@ class SpyderServer {
                         }
                     }
                     catch (e) {
-                        // ignore errors in MVP
+                        console.error('[SPYDER] socket handler error:', e instanceof Error ? e.message : String(e));
                     }
                 });
                 socket.on('close', () => { });
diff --git a/node_modules/@funeste38/spyder/dist/protocol/packet.js b/node_modules/@funeste38/spyder/dist/protocol/packet.js
index 88da8dc..9b8bc76 100644
--- a/node_modules/@funeste38/spyder/dist/protocol/packet.js
+++ b/node_modules/@funeste38/spyder/dist/protocol/packet.js
@@ -32,5 +32,5 @@ function decodePacket(buf) {
     for (let i = 0; i < 4 + length; i++)
         cs ^= buf[i];
     const valid = cs === checksum;
-    return { header: buf[0], type, length, payload, checksum: valid ? checksum : 0 };
+    return { header: buf[0], type, length, payload, checksum, valid };
 }
